{
  "buildCommand": "pnpm --filter @workspace/quran-app run build",
  "outputDirectory": "artifacts/quran-app/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
