[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/quran-app run build"
  publish = "artifacts/quran-app/dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
